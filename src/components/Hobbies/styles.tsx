import { Box } from '@mui/material';
import styled from 'styled-components';

export const HobbyCardContainer = styled(Box)`
    width: auto;
    padding: 5px 15px;
    border: 1px solid #08171E;
    border-radius: 20px;
`